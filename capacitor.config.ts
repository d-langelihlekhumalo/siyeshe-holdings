import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'siyeshe-holdings',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
